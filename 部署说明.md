# OpenClaw AI Agent Framework - 容器化部署

## 🎯 项目概述

OpenClaw 是一个开源的个人 AI 助手框架，支持多平台消息集成（WhatsApp、Telegram、Discord、Slack 等 20+ 平台），可以在您自己的设备上运行。

- ⭐ GitHub Stars: 247,000+
- 🍴 Forks: 47,700+
- 📅 最后更新: 2026年3月

## 📁 项目结构

```
D:\openclaw\
├── .env                    # 环境变量配置文件（已创建，需修改）
├── .env.example            # 环境变量示例文件
├── Dockerfile              # Docker 镜像定义（官方提供）
├── docker-compose.yml      # Docker Compose 配置（官方提供）
├── quick-start.ps1         # PowerShell 快速启动脚本（已创建）
├── quick-start.sh          # Bash 快速启动脚本（已创建）
├── README-DEPLOY.md        # 详细部署文档（已创建）
├── 部署说明.md             # 本文件
├── config/                 # 配置数据目录（已创建）
└── workspace/              # 工作空间目录（已创建）
```

## 🚀 快速开始（3 步）

### 方式一：使用自动化脚本（推荐）

1️⃣ **配置环境变量**

编辑 `.env` 文件，修改以下必需配置：

```bash
# 生成安全令牌（PowerShell）
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})

# 将生成的令牌填入：
OPENCLAW_GATEWAY_TOKEN=你生成的令牌

# 配置至少一个 AI API 密钥：
OPENAI_API_KEY=sk-...
# 或
ANTHROPIC_API_KEY=sk-ant-...
# 或
GEMINI_API_KEY=...
```

2️⃣ **运行快速启动脚本**

打开 PowerShell，切换到项目目录：

```powershell
cd D:\openclaw
.\quick-start.ps1
```

3️⃣ **验证部署**

访问：http://localhost:18789/healthz

应该返回：`{"ok": true}`

### 方式二：手动部署

1️⃣ **配置 .env 文件**（同上）

2️⃣ **构建 Docker 镜像**

```bash
# 基础版
docker build -t openclaw:local .

# 或包含浏览器自动化
docker build --build-arg OPENCLAW_INSTALL_BROWSER=1 -t openclaw:local .
```

3️⃣ **启动服务**

```bash
docker compose up -d
```

4️⃣ **查看日志**

```bash
docker compose logs -f
```

## 🔑 必需配置

在 `.env` 文件中至少需要配置：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `OPENCLAW_GATEWAY_TOKEN` | Gateway 认证令牌 | `abc123...` |
| `OPENAI_API_KEY` 或<br>`ANTHROPIC_API_KEY` 或<br>`GEMINI_API_KEY` | AI 模型 API 密钥 | `sk-...` |

## 📡 配置消息平台（可选）

### Telegram

1. 与 [@BotFather](https://t.me/botfather) 创建机器人
2. 在 `.env` 添加：`TELEGRAM_BOT_TOKEN=your-token`
3. 重启服务：`docker compose restart`

### Discord

1. 在 [Discord Developer Portal](https://discord.com/developers/applications) 创建应用
2. 在 `.env` 添加：`DISCORD_BOT_TOKEN=your-token`
3. 重启服务

## 🛠️ 常用命令

| 操作 | 命令 |
|------|------|
| 启动服务 | `docker compose up -d` |
| 停止服务 | `docker compose down` |
| 查看日志 | `docker compose logs -f` |
| 重启服务 | `docker compose restart` |
| 查看状态 | `docker compose ps` |
| 使用 CLI | `docker compose run --rm openclaw-cli` |
| 健康检查 | `curl http://localhost:18789/healthz` |

## 📊 服务端口

| 端口 | 用途 |
|------|------|
| 18789 | OpenClaw Gateway（WebSocket + HTTP） |
| 18790 | Bridge 端口 |

## 🔐 安全建议

- ✅ 使用强随机 `OPENCLAW_GATEWAY_TOKEN`（至少 32 字符）
- ✅ 不要将 `.env` 文件提交到版本控制
- ✅ 定期备份 `config/` 和 `workspace/` 目录
- ✅ 生产环境使用 HTTPS 反向代理
- ✅ 限制 Gateway 端口的外部访问

## 📚 文档资源

- 📖 详细部署文档：`README-DEPLOY.md`
- 🌐 官方网站：https://openclaw.ai
- 📚 官方文档：https://docs.openclaw.ai
- 💻 GitHub：https://github.com/openclaw/openclaw

## 🐛 故障排查

### 问题：容器无法启动

```bash
# 查看详细日志
docker compose logs openclaw-gateway

# 常见原因：
# 1. 端口被占用 → 修改 .env 中的端口
# 2. 缺少 API 密钥 → 检查 .env 配置
# 3. 目录权限问题 → 确保 config/ 和 workspace/ 可写
```

### 问题：Gateway 无法连接

```bash
# 检查服务状态
docker compose ps

# 测试健康检查
curl http://localhost:18789/healthz

# 确保 .env 中设置了：
OPENCLAW_GATEWAY_BIND=lan
```

### 问题：AI 模型无响应

```bash
# 检查 API 密钥是否正确
docker compose exec openclaw-gateway env | grep API_KEY

# 测试 OpenAI 连接
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer sk-your-key"
```

## 🎉 下一步

1. ✅ 部署完成
2. 🤖 创建您的第一个 AI 代理
3. 📱 连接消息平台（Telegram、Discord 等）
4. 🔧 探索高级功能（浏览器自动化、沙箱隔离等）

---

需要帮助？查看 `README-DEPLOY.md` 获取更详细的文档！🦞
