# 阿里云 Coding Plan 配置指南

## 📋 概述

阿里云 **Coding Plan** 是专门为编程工具（如 OpenClaw、Claude Code）提供的大模型订阅服务，支持多家主流模型，价格实惠，适合国内用户。

### 🎯 核心特点

- ✅ **国内访问友好**：无需科学上网，低延迟
- ✅ **多模型支持**：Qwen3.5、GLM-5、Kimi-K2.5、MiniMax-M2.5 等
- ✅ **价格实惠**：基础版 ¥40/月，高级版 ¥200/月
- ✅ **按请求计费**：而非按 Token 计费
- ✅ **官方支持**：阿里云官方文档完善

### ⚠️ 使用限制

**重要**：Coding Plan 仅限在编程工具中使用，禁止用于：
- ❌ 自动化脚本
- ❌ 自定义应用程序后端
- ❌ 非交互式批量调用

## 🔑 第一步：获取 API Key

### 1. 登录阿里云控制台

访问：https://bailian.console.aliyun.com/coding-plan

### 2. 开通 Coding Plan

选择套餐：
- **Lite 基础版**：¥40/月（适合个人学习）
- **Pro 高级版**：¥200/月（适合专业开发）

### 3. 获取 API Key

开通后，在 Coding Plan 页面获取专属 API Key。

**API Key 格式**：`sk-sp-xxxxx`

⚠️ **注意**：
- Coding Plan 的 API Key（`sk-sp-xxxxx`）与百炼按量计费的 API Key（`sk-xxxxx`）**不互通**
- 请勿混用！

## ⚙️ 第二步：配置 OpenClaw

### 方式一：使用配置文件（推荐）

#### 1. 编辑配置文件

使用文本编辑器打开：
```
D:\openclaw\config\openclaw.json
```

已为您预生成了配置模板！

#### 2. 替换 API Key

找到所有的 `YOUR_API_KEY`，替换为您的实际 API Key：

```json
{
  "models": {
    "providers": {
      "aliyun-coding-qwen": {
        "apiType": "openai-completions",
        "apiKey": "sk-sp-your-actual-key-here",  // 👈 替换这里
        "baseUrl": "https://coding.dashscope.aliyuncs.com/v1",
        ...
      },
      ...
    }
  }
}
```

**提示**：可以为所有 provider 使用同一个 API Key。

#### 3. 选择默认模型（可选）

编辑 `agents.defaults.model.primary`，选择您喜欢的默认模型：

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "qwen3.5-plus"  // 推荐：速度快、效果好
        // 其他选项：
        // "glm-5"          - 智谱 GLM-5
        // "kimi-k2.5"      - Kimi K2.5
        // "minimax-m2.5"   - MiniMax M2.5
        // "qwen3-coder-plus" - Qwen3 Coder Plus（专业代码）
      }
    }
  }
}
```

### 方式二：仅使用环境变量（简化配置）

如果您只想快速测试，可以只配置 `.env` 文件：

编辑 `D:\openclaw\.env`：

```bash
# 取消注释并填入您的 API Key
OPENAI_API_KEY=sk-sp-your-coding-plan-api-key-here
```

**注意**：此方式仅适用于 OpenAI 兼容接口，功能受限。推荐使用方式一。

## 🚀 第三步：启动服务

### 1. 运行部署脚本

```powershell
cd D:\openclaw
.\quick-start.ps1
```

### 2. 等待构建完成

首次构建约需 10-20 分钟。

### 3. 验证部署

访问：http://localhost:18789/healthz

应返回：`{"ok": true}`

## 🎨 第四步：使用 OpenClaw

### Web UI 方式

```bash
# 打开 Web 控制台
docker compose run --rm openclaw-cli dashboard
```

然后在浏览器中访问显示的地址（通常是 http://localhost:18791）

### TUI 方式（终端界面）

```bash
# 打开终端界面
docker compose run --rm openclaw-cli tui
```

### 切换模型

在 TUI 中临时切换模型：
```
/model qwen3.5-plus
/model glm-5
/model kimi-k2.5
```

## 📊 支持的模型列表

| 模型 ID | 模型名称 | 上下文窗口 | 最大输出 | 推荐用途 |
|---------|----------|-----------|----------|----------|
| `qwen3.5-plus` | Qwen3.5 Plus | 128K | 8K | 通用对话、编程（推荐） |
| `qwen3-coder-plus` | Qwen3 Coder Plus | 128K | 8K | 专业代码生成 |
| `qwen3-coder-turbo` | Qwen3 Coder Turbo | 128K | 8K | 快速代码补全 |
| `glm-5` | GLM-5 | 128K | 8K | 智谱最新模型 |
| `glm-4-plus` | GLM-4 Plus | 128K | 8K | GLM-4 增强版 |
| `kimi-k2.5` | Kimi K2.5 | 128K | 8K | 长文本理解 |
| `minimax-m2.5` | MiniMax M2.5 | 245K | 8K | 超长上下文 |

## 🔧 高级配置

### Base URL 说明

阿里云 Coding Plan 提供两种兼容协议：

1. **OpenAI 兼容协议**（推荐）：
   ```
   https://coding.dashscope.aliyuncs.com/v1
   ```

2. **Anthropic 兼容协议**：
   ```
   https://coding.dashscope.aliyuncs.com/apps/anthropic
   ```

当前配置使用 OpenAI 兼容协议。

### 同时使用多个提供商

您可以同时配置阿里云和其他国际提供商：

```json
{
  "models": {
    "providers": {
      "aliyun-coding-qwen": {
        "apiKey": "sk-sp-aliyun-key",
        ...
      },
      "openai": {
        "apiKey": "sk-openai-key",
        ...
      },
      "anthropic": {
        "apiKey": "sk-ant-anthropic-key",
        ...
      }
    }
  }
}
```

### 设置模型优先级

编辑 `agents.defaults.model`：

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "qwen3.5-plus",      // 首选模型
        "fallbacks": [                  // 备选模型（可选）
          "glm-5",
          "kimi-k2.5"
        ]
      }
    }
  }
}
```

## 🐛 故障排查

### 问题 1：API Key 无效

**错误信息**：`401 Unauthorized` 或 `Invalid API Key`

**解决方案**：
1. 确认 API Key 格式为 `sk-sp-xxxxx`
2. 检查是否在正确的页面获取（Coding Plan 页面，非百炼控制台）
3. 确认已成功开通 Coding Plan 订阅

### 问题 2：模型不可用

**错误信息**：`Model not found` 或 `403 Forbidden`

**解决方案**：
1. 检查您的套餐是否支持该模型（Lite vs Pro）
2. 确认模型 ID 拼写正确
3. 访问控制台查看可用模型列表

### 问题 3：请求速率限制

**错误信息**：`429 Too Many Requests`

**解决方案**：
1. 检查套餐的请求限额
2. 考虑升级到 Pro 套餐
3. 添加重试逻辑或延迟

### 问题 4：连接超时

**解决方案**：
1. 检查网络连接
2. 确认 Base URL 正确
3. 尝试切换 DNS（如 8.8.8.8）

## 📚 相关资源

- 🌐 阿里云 Coding Plan 官方页面：https://bailian.console.aliyun.com/coding-plan
- 📖 官方文档：https://help.aliyun.com/zh/model-studio/openclaw-coding-plan
- 🔑 获取 API Key：https://help.aliyun.com/zh/model-studio/get-api-key
- ❓ 常见问题：https://help.aliyun.com/zh/model-studio/coding-plan-faq
- 📊 模型服务（灵积）：https://dashscope.aliyun.com/

## 💡 使用技巧

### 1. 选择合适的模型

- **通用编程任务**：`qwen3.5-plus`（速度快、效果好）
- **专业代码生成**：`qwen3-coder-plus`（针对代码优化）
- **快速补全**：`qwen3-coder-turbo`（速度最快）
- **复杂推理**：`glm-5`（逻辑能力强）
- **长文本处理**：`minimax-m2.5`（245K 上下文）

### 2. 优化性能

- 使用 `qwen3-coder-turbo` 进行快速迭代
- 复杂任务使用 `qwen3.5-plus` 或 `glm-5`
- 启用模型缓存（在 `openclaw.json` 中配置）

### 3. 成本控制

- Coding Plan 按请求次数计费，不按 Token
- 合理使用上下文，避免不必要的长提示
- 选择 Lite 套餐即可满足大多数个人需求

## 🎉 快速测试

配置完成后，可以通过以下方式测试：

```bash
# 启动 TUI
docker compose run --rm openclaw-cli tui

# 发送测试消息
你好，请用中文介绍一下你自己

# 切换模型测试
/model glm-5
你好，请用中文介绍一下你自己
```

---

**配置完成！享受阿里云 Coding Plan 带来的高效编程体验！** 🚀

需要帮助？查看[官方文档](https://help.aliyun.com/zh/model-studio/openclaw-coding-plan)或提交 Issue。
